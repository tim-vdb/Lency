/// *
/// LOCALISATION - LIEUX & SPOTS
model MapLocation {
  id          String    @id @default(cuid())
  name        String
  latitude    Float
  longitude   Float
  description String?
  createdAt   DateTime  @default(now())
  Projects    Project[]
}

