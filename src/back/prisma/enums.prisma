enum Role {
  ADMIN
  USER
  MEMBER
  PREMIUM
}

enum ResourceType {
  ASSET
  TUTORIAL
  LINK
}

enum ProjectStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}

enum Visibility {
  PUBLIC
  PRIVATE
  MEMBERS_ONLY
}

enum GalleryType {
  EVENT
  GALLERY
}

enum TypeEvent {
  PHYSICAL
  VIRTUAL
}

enum PostFormat {
  DESKTOP
  MOBILE
  AUDIO
  TEXT
}

enum TypeEvent {
    PHYSICAL
    VIRTUAL
}